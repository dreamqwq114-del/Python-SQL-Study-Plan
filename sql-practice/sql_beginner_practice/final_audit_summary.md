# 最终审计总结

## 审计流程

- 已成功调用两个互相独立的 Subagent：
  - SQL 正确性审计员；
  - 教学质量审计员。
- 首轮审计后，主 Agent 阅读了两份完整报告，修复全部严重和一般问题，并逐条处理建议。
- 修复后再次调用 SQL 正确性审计 Subagent 完成第二轮复查。
- 第二轮发现 1 个新的一般问题，主 Agent 修复后由同一审计员再次确认。

## 第一轮问题数量

- 严重：2
- 一般：11
- 建议：4
- 合计：17

其中：

- SQL 正确性审计：严重 2、一般 2、建议 1；
- 教学质量审计：严重 0、一般 9、建议 3。

## 修复结果

- 第一轮 17 项全部修复。
- 第二轮新增 1 个一般问题，已修复并复核。
- 最终共修复 18 个问题。
- 未采纳建议：无。首轮 4 条建议均已采纳。
- 第二轮最终未修复：严重 0、一般 0、建议 0。

主要修复包括：

- 修正第 6 天部门人数漏算无项目员工的问题；
- 为全部答案文件补充练习数据库安全断言；
- 将固定种子数据放入事务，并增加固定基线断言；
- 消除日期边界表述歧义；
- 为项目指标的 `project_id` 增加唯一约束；
- 降低第 7～8 天难度跳跃，补齐示例和结果说明；
- 补齐旧知识题的返回列、预计行数和自查特征；
- 增加 `COUNT(*)` 与 `COUNT(column)` 的 NULL 改错题；
- 区分 JOIN 语法报错与笛卡尔积逻辑错误；
- 移除最终课中未教学的 `UNION ALL`；
- 减弱阶段测试提示，避免直接泄露关键解法；
- 修正第二轮发现的最终题重复计数表述冲突。

## SQL 执行边界

- **仅静态审计，未实际执行。**
- 当前环境未发现 `sqlcmd`、`Invoke-Sqlcmd` 或本机 SQL Server 服务。
- 没有安装额外软件，没有修改系统环境，也没有连接或操作 `ecology0110` 或任何业务数据库。
- 因此不能声称这些 SQL 已在真实 SQL Server 中运行通过。

## 最终静态检查

- 目录共有 34 个要求文件，其中根目录 21 个、`answers` 目录 13 个。
- 课程题目与答案题号逐文件对应。
- 表名、字段名、别名、外键、自关联和联合主键静态检查一致。
- 固定数据设计为 5 个部门、20 名员工、14 个项目、12 条指标、37 条参与关系。
- 可执行 SQL 中未发现 `DROP DATABASE`、`DROP TABLE`、窗口函数、存储过程、触发器、游标、动态 SQL或递归 CTE。
- 实际 `UPDATE`、`DELETE` 均有数据库断言、预览 `SELECT`、精确 `WHERE`、事务和默认 `ROLLBACK`。

## 尚存限制

- 未经真实 SQL Server 执行，运行时语法、客户端批处理行为和实际结果仍需在隔离的本地练习库中验证。
- 初始化脚本不会自动删除或重置已有数据；若 `sql_beginner_practice` 已存在且不是固定基线，脚本会停止并保护已有内容。
- DBeaver 的执行快捷键可能因个人设置不同，请以本机界面显示为准。

## 第一天推荐顺序

1. 运行 `00_create_database.sql`。
2. 用 `SELECT DB_NAME()` 确认当前数据库是 `sql_beginner_practice`。
3. 独立完成 `00_diagnostic_test.sql`。
4. 查看 `diagnostic_review.md`，判断哪些知识不稳定。
5. 再开始 `01_select_basics.sql`。
6. 第一天不要打开全部答案文件。

USE mysql_beginner_practice;

SELECT DATABASE() AS current_database;

-- ==================================================
-- 练习 1：按薪资从低到高查看员工
-- ==================================================
-- 难度：模仿
-- 目标：
-- 显示 employee_name、salary，并按薪资升序排列。
-- 应返回：
-- 2 列；所有员工；薪资从低到高。
-- 使用知识：
-- ORDER BY、ASC
-- 验证方式：
-- 检查相邻薪资，不要只看第一行。
-- 提示：
-- 对 salary 使用升序。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 2：按开始日期查看项目
-- ==================================================
-- 难度：模仿
-- 目标：
-- 显示 project_name、start_date，并按开始日期从晚到早排列。
-- 应返回：
-- 2 列；所有项目；日期降序。
-- 使用知识：
-- ORDER BY、DESC
-- 验证方式：
-- 检查相邻日期是否从晚到早。
-- 提示：
-- 对 start_date 使用降序。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 3：稳定排列部门内员工
-- ==================================================
-- 难度：基础变形
-- 目标：
-- 显示 employee_name、department_id、hire_date。
-- 先按 department_id 升序，同一部门内按 hire_date 从早到晚，
-- 日期相同时按 employee_name 升序。
-- 应返回：
-- 3 列；所有员工；具有三层稳定顺序。
-- 使用知识：
-- 多列 ORDER BY
-- 验证方式：
-- 先看部门，再看同部门日期和同日期姓名。
-- 提示：
-- 排序规则按业务优先级从左到右写。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 4：筛选 2024 年起开始的项目
-- ==================================================
-- 难度：基础变形
-- 目标：
-- 查询 start_date 不早于 2024-01-01 的项目名称和开始日期，
-- 按开始日期升序，同日按 project_id 升序。
-- 应返回：
-- 2 列；没有早于边界的日期；日期从早到晚。
-- 使用知识：
-- 日期条件、WHERE、多列排序
-- 验证方式：
-- 检查边界、日期方向和同日顺序。
-- 提示：
-- 日期使用标准格式并加单引号。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 5：筛选低预算暂停项目
-- ==================================================
-- 难度：基础变形
-- 目标：
-- 查询状态为“暂停”且预算低于 500000 的项目。
-- 显示 project_name、project_status、budget，按预算降序。
-- 应返回：
-- 3 列；状态统一；预算严格低于 500000。
-- 使用知识：
-- 文本条件、数值条件、ORDER BY
-- 验证方式：
-- 同时检查状态、预算上限和排序方向。
-- 提示：
-- 两个筛选条件同时成立，再排序。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 6：制作新员工时间清单
-- ==================================================
-- 难度：独立
-- 目标：
-- 查询 2022-01-01 到 2023-12-31 期间入职的员工。
-- 显示 employee_id、employee_name、hire_date。
-- 按 hire_date 从晚到早，同日按 employee_id 升序。
-- 应返回：
-- 3 列；日期包含两个边界，并按要求稳定排序。
-- 使用知识：
-- 日期范围、WHERE、多列排序
-- 验证方式：
-- 检查最早和最晚日期、方向及同日编号。
-- 提示：
-- 自行决定范围条件与两个排序规则。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 7：制作已完成项目档案顺序
-- ==================================================
-- 难度：独立
-- 目标：
-- 查询“已完成”且 end_date 非空的项目。
-- 显示 project_name、start_date、end_date、budget。
-- 按 end_date 从晚到早；结束日期相同时按 budget 从高到低，
-- 仍相同时按 project_name 升序。
-- 应返回：
-- 4 列；无 NULL 结束日期；三层稳定顺序。
-- 使用知识：
-- 文本条件、IS NOT NULL、多列排序
-- 验证方式：
-- 检查状态含义、NULL、日期与并列顺序。
-- 提示：
-- 筛选完成后，按三个优先级排序。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 8：改正最早入职员工查询
-- ==================================================
-- 难度：改错
-- 目标：
-- 原意是查询 2020-01-01 之后入职的员工，并按最早入职优先，
-- 相同日期时 employee_id 较小者优先。
-- 错误 SQL（可以运行，但排序方向错误）：
-- SELECT employee_id, employee_name, hire_date
-- FROM employees
-- WHERE hire_date >= '2020-01-01'
-- ORDER BY hire_date DESC;
-- 请回答：
-- 1. 原 SQL 是否报错？
-- 2. 结果含义为什么不正确或不稳定？
-- 3. 应如何修改？为什么？
-- 4. 修改后结果应有什么特征？
-- 应返回：
-- 3 列；日期不早于边界；最早日期在前；同日编号升序。
-- 使用知识：
-- 日期条件、排序方向、第二排序列
-- 验证方式：
-- 检查第一行日期、相邻日期和同日编号。
-- 提示：
-- “最早优先”需要升序，并为并列日期增加规则。
-- 在下方填写修改后的 SQL 和解释：



-- 本章结束自检：
-- [ ] 我先完成了 lesson.md 中的所有示例
-- [ ] 我没有看答案完成至少 6 道题
-- [ ] 我能解释每条 SQL 的作用
-- [ ] 我检查了返回列数
-- [ ] 我检查了返回行数
-- [ ] 我检查了 NULL 和重复
-- [ ] 我把真正做错的题记录进 mistake_log.md

USE mysql_beginner_practice;

SELECT DATABASE() AS current_database;

-- ==================================================
-- 练习 1：显示全部项目及其投资收益率
-- ==================================================

-- 难度：模仿

-- 目标：
-- 显示全部项目的编号、名称和投资收益率。
-- 没有指标记录的项目也必须保留。
-- 按项目编号升序排列。

-- 应返回：
-- 3 列：project_id、project_name、investment_return_rate。
-- 覆盖全部项目，右表字段可能为 NULL。

-- 使用知识：
-- LEFT JOIN、无匹配行

-- 验证方式：
-- 将项目覆盖范围与项目总数比较。
-- 检查无指标项目是否仍出现。

-- 提示：
-- 把必须全部保留的 projects 放在左边。

-- 在下方填写 SQL：



-- ==================================================
-- 练习 2：显示全部员工及其项目角色
-- ==================================================

-- 难度：模仿

-- 目标：
-- 显示全部员工的编号、姓名、项目编号和参与角色。
-- 没有项目的员工也必须保留。
-- 按员工编号、项目编号升序排列。

-- 应返回：
-- 4 列：employee_id、employee_name、project_id、role_name。
-- 一名员工可能有多行，无项目员工的右表列为 NULL。

-- 使用知识：
-- LEFT JOIN、一对多

-- 验证方式：
-- 检查无项目员工是否出现。
-- 检查员工重复是否来自不同项目。

-- 提示：
-- 员工左连接 employee_projects 即可取得项目编号和角色。

-- 在下方填写 SQL：



-- ==================================================
-- 练习 3：查找没有指标的项目
-- ==================================================

-- 难度：基础变形

-- 目标：
-- 显示没有 project_metrics 记录的项目编号和名称。
-- 按项目编号升序排列。

-- 应返回：
-- 2 列：project_id、project_name。
-- 只包含无指标记录的项目。

-- 使用知识：
-- LEFT JOIN、IS NULL

-- 验证方式：
-- 用项目编号检查 project_metrics 中是否确实没有对应行。
-- 确认结果没有重复。

-- 提示：
-- 筛选右表的连接键为空。

-- 在下方填写 SQL：



-- ==================================================
-- 练习 4：查找没有参与项目的员工
-- ==================================================

-- 难度：基础变形

-- 目标：
-- 显示没有任何项目参与记录的员工编号、姓名和职位。
-- 按员工编号升序排列。

-- 应返回：
-- 3 列：employee_id、employee_name、job_title。
-- 只包含无项目员工。

-- 使用知识：
-- LEFT JOIN、IS NULL、中间表

-- 验证方式：
-- 检查这些员工编号在 employee_projects 中是否不存在。
-- 确认不是检查允许为空的业务字段。

-- 提示：
-- 从员工出发，检查中间表连接键。

-- 在下方填写 SQL：



-- ==================================================
-- 练习 5：统计每个部门的不重复项目数
-- ==================================================

-- 难度：基础变形

-- 目标：
-- 部门依次连接项目和参与记录。
-- 显示部门名称及不重复项目数，保留所有部门。
-- 按部门编号升序排列。

-- 应返回：
-- 2 列：department_name、project_count。
-- 每个部门一行，项目不能因多人参与被重复计数。

-- 使用知识：
-- LEFT JOIN、GROUP BY、COUNT(DISTINCT ...)

-- 验证方式：
-- 与直接按 projects.department_id 统计的项目数比较。
-- 检查每个部门是否都出现。

-- 提示：
-- 连接参与表后，一个项目可能出现多次。

-- 在下方填写 SQL：



-- ==================================================
-- 练习 6：统计每名员工参与的项目数
-- ==================================================

-- 难度：独立

-- 目标：
-- 显示全部员工的编号、姓名和参与项目数。
-- 没有项目的员工计数为 0。
-- 按员工编号升序排列。

-- 应返回：
-- 3 列：employee_id、employee_name、project_count。
-- 每名员工一行。

-- 使用知识：
-- LEFT JOIN、GROUP BY、COUNT

-- 验证方式：
-- 检查无项目员工是否为 0 而不是 1。
-- 抽查参加多个项目的员工。

-- 提示：
-- 计数右表中只有匹配时才非 NULL 的项目编号。

-- 在下方填写 SQL：



-- ==================================================
-- 练习 7：制作全部项目的参与人数清单
-- ==================================================

-- 难度：独立

-- 目标：
-- 显示全部项目的编号、名称和不重复参与员工数。
-- 没有参与人员的项目计数为 0。
-- 按参与人数降序、项目编号升序排列。

-- 应返回：
-- 3 列：project_id、project_name、employee_count。
-- 每个项目一行。

-- 使用知识：
-- LEFT JOIN、COUNT(DISTINCT ...)、GROUP BY、多列排序

-- 验证方式：
-- 检查无参与人员项目是否保留且为 0。
-- 检查项目没有因参与记录而重复成多行。

-- 提示：
-- 必须保留项目，并按项目分组后统计员工编号。

-- 在下方填写 SQL：



-- ==================================================
-- 练习 8：改正丢失项目的 LEFT JOIN
-- ==================================================

-- 难度：改错

-- 目标：
-- 原意是保留全部项目，仅在右表匹配 110kV 指标。
-- 判断下面 SQL 是否报错、结果是否正确，并修复。

-- 错误 SQL（可以运行，但会丢掉无匹配项目）：
-- SELECT p.project_name, pm.voltage_level
-- FROM projects AS p
-- LEFT JOIN project_metrics AS pm
--     ON p.project_id = pm.project_id
-- WHERE pm.voltage_level = '110kV';

-- 请回答：
-- 1. 原 SQL 是否会报错？
-- 2. 为什么 WHERE 会过滤无匹配项目？
-- 3. 筛选条件应移到哪里？为什么？
-- 4. 修改后结果应有什么特征？

-- 应返回：
-- 2 列：project_name、voltage_level。
-- 全部项目都保留，非 110kV 或无指标项目的右表列为 NULL。

-- 使用知识：
-- LEFT JOIN、ON、WHERE、右表筛选陷阱

-- 验证方式：
-- 将项目覆盖范围与项目总数比较。
-- 检查只匹配 110kV 指标。

-- 提示：
-- 要限制匹配而不删除左表行，应调整连接条件。

-- 在下方填写修改后的 SQL 和解释：



-- 本章结束自检：
-- [ ] 我先完成了 lesson.md 中的所有示例
-- [ ] 我没有看答案完成至少 6 道题
-- [ ] 我能解释每条 SQL 的作用
-- [ ] 我检查了返回列数
-- [ ] 我检查了返回行数
-- [ ] 我检查了 NULL 和重复
-- [ ] 我把真正做错的题记录进 mistake_log.md

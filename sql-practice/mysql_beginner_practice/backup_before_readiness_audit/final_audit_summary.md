# 最终审计摘要

## 结论

- 重构完成。
- 第 1 章先作为样板完成，并经过独立教学 Subagent 审计、修复和复核后，才展开其他章节。
- Subagent 功能成功启用。
- 共调用 6 个独立 Subagent：1 个第 1 章教学审计、3 个分章内容构建、1 个最终 SQL 正确性审计、1 个最终教学质量审计。
- 最终正确性复核：严重 0、一般 0、未采纳建议 1。
- 最终教学复核：严重 0、一般 0、建议 0。

## 备份

- 备份目录：`backup_before_refactor/`。
- 备份文件数：34。
- 建立备份时，源文件与备份文件数量一致，SHA-256 不一致文件数为 0。
- 旧版根目录重复文件已从新结构中移除；完整旧内容仍保存在备份目录，可恢复。

## 第 1 章样板审计

- 独立角色：SQL 基础不牢大学生视角。
- 第一次发现：严重 0、一般 5、建议 2。
- 修复内容：拆分概念、重新设计练习梯度、修复提示泄题、增强答案验证、调整示例数量和时间说明。
- 后续最终教学审计又要求补充 `SELECT *` 可运行观察示例，现已完成。
- 样板最终状态：内容审计合格。

## 最终第一轮双审计

### MySQL 正确性审计

- 严重：0。
- 一般：1。
- 建议：2。

主要发现：

- setup 的初步状态曾用 `>=` 检查行数，与固定基线的精确检查不一致；
- `information_schema.tables.table_rows` 对 InnoDB 是估算值；
- `VALUES(column)` 在较新的 MySQL 8.0 中已标记弃用，但目前仍是有效语法。

### 初学者教学质量审计

- 严重：0。
- 一般：5。
- 建议：0。

主要发现：

- 45～60 分钟与 8 题每题至少 5～10 分钟存在时间下限冲突；
- 诊断页曾引用不存在的普通章节文件；
- 第 2 章曾在一个示例中同时引入 `IN`、`BETWEEN`、`LIKE`；
- `LIKE` 的一处预测方向写反；
- 第 1 章 `SELECT *` 和第 6 章 LEFT JOIN 右表筛选陷阱缺少独立操作脚手架。

### 第一轮合计

- 严重：0。
- 一般：6。
- 建议：2。

## 主 Agent 修复

- 修复全部 6 个一般问题。
- 采纳并修复 1 条建议：不再展示 InnoDB 的估算行数，只列出表名并用 `COUNT(*)` 精确核对。
- 共修复 7 个第一轮问题或建议。
- 未采纳 1 条建议：继续使用 `VALUES(column)` 完成幂等基线写入。

未采纳原因：

- 该写法在 MySQL 8.0 中仍可执行，只是被标记为弃用；
- 当前可用实测环境 MariaDB 10.4.32 不支持建议替换的新式行别名写法；
- 保留当前写法可以让同一 setup 脚本在当前 XAMPP 环境和 MySQL 8.0 目标语法之间保持可运行；
- 未来若课程只运行在较新 MySQL 8.0，可再迁移到新式别名语法。

## 第二轮正确性复核

- 第一轮一般问题已修复。
- 已采纳的 `table_rows` 建议已修复。
- 54 个 lesson 示例与 54 个最小修改答案一一对应。
- 80 道正式练习与 80 份练习答案一一对应。
- 第二轮新增问题：0。
- 最终未解决：严重 0、一般 0、建议 1。

## 实际数据库运行

- 实际连接：成功。
- 客户端：`D:\xampp\mysql\bin\mysql.exe`。
- 实际服务器兼容环境：XAMPP MariaDB 10.4.32。
- 实际数据库：仅 `mysql_beginner_practice`。
- 第二轮独立复跑：71 个执行单元。
- SQL 报错：0。
- 最终 `baseline_status`：`PASS`。
- 第 9 章运行后固定数据无残留修改。
- 未使用 `DROP DATABASE`，未操作其他数据库。

重要边界：

- 本机没有发现真正的 MySQL 8.0 服务。
- 因此不能声称“已在真实 MySQL 8.0 中全部运行通过”。
- 当前结论是：MySQL 8.0 静态方言审计通过；MariaDB 10.4.32 实际执行通过。

## 教学复核

- 第一轮 5 个一般教学问题全部确认修复。
- 第 1、2、6 章分别有 6、7、6 个示例，其余章节各有 5 个，均在 5～8 个范围内。
- 每个示例均保留预测、观察、最小修改和对应答案。
- 每章 8 道题、2/3/2/1 分层、8 组三级提示和 8 份答案未受影响。
- 新增教学问题：0。
- 最终未解决：严重 0、一般 0、建议 0。

## 当前限制

1. 没有真实 MySQL 8.0 运行环境，只完成 MySQL 8.0 静态审计和 MariaDB 10.4.32 实跑。
2. 整章通常需要 65～90 分钟。一次学习时段控制在 45～60 分钟，需要分两个时段；这是“8 题且每题至少思考 5～10 分钟”和“整章 45～60 分钟”之间的时间下限冲突。
3. setup 中的 `VALUES(column)` 是有效但已弃用的 MySQL 8.0 写法，保留用于当前 MariaDB/MySQL 双环境兼容。

## 第一天只打开

1. `00_setup/lesson.md`
2. `00_setup/create_database.sql`
3. `00_setup/verify_database.sql`
4. `00_diagnostic/lesson.md`
5. `00_diagnostic/diagnostic.sql`

第一天不要打开全部章节，也不要提前打开任何 `answers.sql`。

USE mysql_beginner_practice;

SELECT DATABASE() AS current_database;

-- ==================================================
-- 练习 1：用 CTE 查询高薪员工
-- ==================================================

-- 难度：模仿
-- 目标：
-- 在 CTE 中选出 salary >= 15000 的员工，再显示姓名和薪资。
-- 按薪资降序、员工编号升序排列。
-- 应返回：
-- 2 列：employee_name、salary；只包含满足条件的员工。
-- 使用知识：
-- 非递归 CTE、WHERE、ORDER BY
-- 验证方式：
-- 检查最低薪资是否仍达到 15000，并检查排序和重复。
-- 提示：
-- CTE 中保留排序需要用到的员工编号。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 2：查找有指标记录的项目
-- ==================================================

-- 难度：模仿
-- 目标：
-- 使用 EXISTS 查找至少有一条 project_metrics 记录的项目。
-- 显示 project_id、project_name，按项目编号升序。
-- 应返回：
-- 2 列；每个符合条件的项目最多一行。
-- 使用知识：
-- EXISTS、关联子查询
-- 验证方式：
-- 抽查结果项目是否存在指标，并确认没有重复。
-- 提示：
-- 外层项目编号要与指标表的项目编号关联。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 3：查找没有项目参与记录的员工
-- ==================================================

-- 难度：基础变形
-- 目标：
-- 使用 NOT EXISTS 查找完全没有 employee_projects 记录的员工。
-- 显示员工编号、姓名和职位，按员工编号升序。
-- 应返回：
-- 3 列；只包含没有参与项目的员工。
-- 使用知识：
-- NOT EXISTS、关联子查询
-- 验证方式：
-- 检查每名结果员工在参与表中都找不到记录。
-- 提示：
-- 判断的是员工编号是否没有匹配。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 4：用 CTE 整理进行中的项目
-- ==================================================

-- 难度：基础变形
-- 目标：
-- 在 CTE 中筛选“进行中”的项目，并保留项目编号、名称、预算。
-- 主查询显示名称和预算，按预算降序、项目编号升序排列。
-- 应返回：
-- 2 列；只包含进行中的项目。
-- 使用知识：
-- 非递归 CTE、WHERE、ORDER BY
-- 验证方式：
-- 检查状态筛选、列数和稳定排序。
-- 提示：
-- 主查询需要使用项目编号排序，因此 CTE 中不能漏掉它。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 5：显示缺失的项目经理
-- ==================================================

-- 难度：基础变形
-- 目标：
-- 显示项目编号、项目名称和项目经理编号。
-- 项目经理编号为空时显示文本“未分配”，非空编号也转换为文本。
-- 按项目编号升序。
-- 应返回：
-- 3 列；所有项目都保留，显示列中没有 NULL。
-- 使用知识：
-- CAST、COALESCE
-- 验证方式：
-- 检查无经理项目是否仍在，且原字段没有被修改。
-- 提示：
-- 先把编号转换为字符，再为 NULL 提供文本。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 6：查找没有参与人员的项目
-- ==================================================

-- 难度：独立
-- 目标：
-- 找出没有任何员工参与记录的项目。
-- 显示项目编号、项目名称、项目状态，按项目编号升序。
-- 应返回：
-- 3 列；每个项目一行。
-- 使用知识：
-- NOT EXISTS
-- 验证方式：
-- 到 employee_projects 中反查结果项目，确认匹配数为 0。
-- 提示：
-- 选择能表达“没有匹配”的存在性判断。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 7：制作项目指标可读清单
-- ==================================================

-- 难度：独立
-- 目标：
-- 显示项目编号、电压等级和投资收益率。
-- 缺失电压等级显示“未填写”，缺失收益率显示 0。
-- 按项目编号升序。
-- 应返回：
-- 3 列；每条指标记录一行，显示列中不再有 NULL。
-- 使用知识：
-- COALESCE、排序
-- 验证方式：
-- 检查 NULL 的显示、列类型和原表值。
-- 提示：
-- 两个可空字段分别处理，替代值应符合字段含义。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 8：修复失去关联的 EXISTS
-- ==================================================

-- 难度：改错
-- 目标：
-- 原意是查找有参与人员的项目。判断下面 SQL：
-- 1. 是否报错；
-- 2. 即使能运行，结果是否正确；
-- 3. 应如何修改；
-- 4. 为什么这样修改；
-- 5. 修改后结果应有什么特征。
--
-- 错误 SQL（只阅读，不要把它当作正确答案）：
-- SELECT p.project_id, p.project_name
-- FROM projects AS p
-- WHERE EXISTS
-- (
--     SELECT 1
--     FROM employee_projects AS ep
-- )
-- ORDER BY p.project_id;
--
-- 应返回：
-- 2 列；只保留至少有一条参与记录的项目，每个项目一行。
-- 使用知识：
-- EXISTS、内外层关联
-- 验证方式：
-- 检查无参与人员的项目是否被排除。
-- 提示：
-- 子查询必须说明“哪一个项目”的参与记录。
-- 在下方写判断、修改后的 SQL 和原因：


-- 本章结束自检：
-- [ ] 我先完成了 lesson.md 中的所有示例
-- [ ] 我没有看答案完成至少 6 道题
-- [ ] 我能解释每条 SQL 的作用
-- [ ] 我检查了返回列数
-- [ ] 我检查了返回行数
-- [ ] 我检查了 NULL 和重复
-- [ ] 我把真正做错的题记录进 mistake_log.md

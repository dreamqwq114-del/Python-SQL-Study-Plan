USE mysql_beginner_practice;

SELECT DATABASE() AS current_database;

-- ==================================================
-- 练习 1：显示员工及其部门
-- ==================================================

-- 难度：模仿

-- 目标：
-- 显示员工编号、员工姓名和所属部门名称。
-- 按员工编号升序排列。

-- 应返回：
-- 3 列：employee_id、employee_name、department_name。
-- 每名有匹配部门的员工一行。

-- 使用知识：
-- 两表 INNER JOIN、主键、外键

-- 验证方式：
-- 检查列数以及员工和部门的对应关系。
-- 将行数与员工总数比较。

-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 2：显示项目及其所属部门
-- ==================================================

-- 难度：模仿

-- 目标：
-- 显示项目名称、项目状态和所属部门名称。
-- 按项目编号升序排列。

-- 应返回：
-- 3 列：project_name、project_status、department_name。
-- 每个有匹配部门的项目一行。

-- 使用知识：
-- 两表 INNER JOIN、表别名

-- 验证方式：
-- 检查项目是否重复。
-- 检查返回行数是否与项目总数一致。

-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 3：显示项目经理和项目预算
-- ==================================================

-- 难度：基础变形

-- 目标：
-- 显示项目名称、经理姓名和预算。
-- 只保留能匹配到项目经理的项目。
-- 按预算降序、项目编号升序排列。

-- 应返回：
-- 3 列：project_name、manager_name、budget。
-- 行数应少于项目总数。

-- 使用知识：
-- INNER JOIN、AS、多列排序

-- 验证方式：
-- 检查是否没有 manager_name 为 NULL 的行。
-- 检查预算排序和缺失项目。

-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 4：显示项目、部门和经理
-- ==================================================

-- 难度：基础变形

-- 目标：
-- 显示项目编号、项目名称、部门名称和经理姓名。
-- 只保留部门和经理都能匹配的项目。
-- 按项目编号升序排列。

-- 应返回：
-- 4 列：project_id、project_name、department_name、manager_name。
-- 每个匹配项目一行。

-- 使用知识：
-- 三表 INNER JOIN、表别名

-- 验证方式：
-- 分别核对两个连接条件。
-- 检查是否漏掉无经理项目以及是否有异常重复。

-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 5：显示参与人员及其工时
-- ==================================================

-- 难度：基础变形

-- 目标：
-- 通过 employee_projects 显示项目名称、员工姓名和工作时数。
-- 按项目编号升序、员工编号升序排列。

-- 应返回：
-- 3 列：project_name、employee_name、working_hours。
-- 每行代表一条员工参与项目的关系。

-- 使用知识：
-- 三表 INNER JOIN、中间表、一对多

-- 验证方式：
-- 检查返回行数是否与参与关系数一致。
-- 检查项目名称重复是否由不同员工造成。

-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 6：制作参与项目的员工清单
-- ==================================================

-- 难度：独立

-- 目标：
-- 显示员工编号、员工姓名、项目名称和参与角色。
-- 只显示至少有一条项目参与记录的员工。
-- 按员工编号、项目编号升序排列。

-- 应返回：
-- 4 列：employee_id、employee_name、project_name、role_name。
-- 同一员工参加多个项目时会有多行。

-- 使用知识：
-- 三表 INNER JOIN、关系含义、稳定排序

-- 验证方式：
-- 检查每行是否代表不同的员工—项目关系。
-- 检查无项目员工是否被排除。

-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 7：查询带有指标的项目
-- ==================================================

-- 难度：独立

-- 目标：
-- 显示项目编号、项目名称、管理费和投资收益率。
-- 只保留有项目指标记录的项目。
-- 按项目编号升序排列。

-- 应返回：
-- 4 列：project_id、project_name、management_fee、investment_return_rate。
-- 行数应少于项目总数。

-- 使用知识：
-- 两表 INNER JOIN、主键与外键

-- 验证方式：
-- 检查无指标项目是否被排除。
-- 注意指标字段本身仍可能为 NULL。

-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 8：改正异常的员工—部门连接
-- ==================================================

-- 难度：改错

-- 目标：
-- 判断下面的 SQL 是否报错、结果是否正确，并改成正确的员工—部门连接。

-- 错误 SQL（可以运行，但连接含义错误）：
-- SELECT e.employee_name, d.department_name
-- FROM employees AS e
-- INNER JOIN departments AS d
--     ON e.employee_id = d.department_id;

-- 请回答：
-- 1. 原 SQL 是否会报错？
-- 2. 即使能运行，结果为什么不正确？
-- 3. 应如何修改连接条件？为什么？
-- 4. 修改后结果应有什么特征？

-- 应返回：
-- 2 列：employee_name、department_name。
-- 每名有匹配部门的员工一行。

-- 使用知识：
-- 主键、外键、JOIN 条件、运行成功不等于结果正确

-- 验证方式：
-- 检查返回行数是否合理。
-- 抽查员工是否对应正确部门。

-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写修改后的 SQL 和解释：



-- 本章结束自检：
-- [ ] 我先完成了 lesson.md 中的所有示例
-- [ ] 我没有看答案完成至少 6 道题
-- [ ] 我能解释每条 SQL 的作用
-- [ ] 我检查了返回列数
-- [ ] 我检查了返回行数
-- [ ] 我检查了 NULL 和重复
-- [ ] 我把真正做错的题记录进 mistake_log.md

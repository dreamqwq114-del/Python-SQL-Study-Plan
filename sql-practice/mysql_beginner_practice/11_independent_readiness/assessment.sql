USE mysql_beginner_practice;

SELECT DATABASE() AS current_database;

-- 独立实践测评：6 题，100 分，90 分钟。
-- 本文件不提供提示。请复制后在自己的作答文件中填写。

-- ==================================================
-- 第 1 题：需要跟进的未结束项目（10 分）
-- ==================================================

-- 业务目标：
-- 找出结束日期为空，并且状态为“待启动”或“暂停”的项目。
-- 显示项目编号、项目名称、项目状态、结束日期和预算。
-- 按预算从高到低排列；预算相同时按项目编号升序。

-- 结果验证特征：
-- 1. 结果共 5 列。
-- 2. end_date 全部为 NULL。
-- 3. 状态只能是“待启动”或“暂停”。
-- 4. 排序稳定。

-- 在下方填写 SQL：



-- ==================================================
-- 第 2 题：项目状态完整度统计（15 分）
-- ==================================================

-- 业务目标：
-- 按项目状态统计：
-- 1. 项目总数 project_count；
-- 2. 已填写结束日期的项目数 projects_with_end_date；
-- 3. 平均预算 average_budget。
-- 只保留项目数不少于 3 的状态。
-- 按项目数降序，项目数相同时按状态升序。

-- 结果验证特征：
-- 1. 每种符合条件的状态一行。
-- 2. projects_with_end_date 不得大于 project_count。
-- 3. 结束日期为 NULL 的项目不能计入 projects_with_end_date。
-- 4. 只保留项目数不少于 3 的组。

-- 在下方填写 SQL：



-- ==================================================
-- 第 3 题：项目、部门和经理清单（20 分）
-- ==================================================

-- 业务目标：
-- 查询已经分配项目经理的项目。
-- 显示项目编号、项目名称、部门名称、经理姓名和项目状态。
-- 按部门编号升序，再按项目编号升序。

-- 结果验证特征：
-- 1. 结果共 5 列。
-- 2. 每个项目最多一行。
-- 3. 没有项目经理的项目不应返回。
-- 4. 部门和经理必须来自正确的关联记录。

-- 在下方填写 SQL：



-- ==================================================
-- 第 4 题：查找资料不完整的项目（20 分）
-- ==================================================

-- 业务目标：
-- 对每个项目统计不重复参与人数，并判断投资收益率是否已填写。
-- 只返回“没有参与人员”或“投资收益率为空”的项目。
-- 显示项目编号、项目名称、participant_count 和 roi_status。
-- roi_status 使用“收益率已填”或“收益率缺失”。
-- 所有目标项目每个只出现一行，按项目编号升序。

-- 结果验证特征：
-- 1. 结果共 4 列。
-- 2. 没有参与人员的项目 participant_count 为 0。
-- 3. 项目不能因参与人员连接而重复。
-- 4. 至少满足“无人参与”或“收益率缺失”中的一项。
-- 5. 应包含“有参与人员但收益率缺失”的项目。

-- 在下方填写 SQL：



-- ==================================================
-- 第 5 题：高于平均收益率的项目分析（20 分）
-- ==================================================

-- 业务目标：
-- 使用一个非递归 CTE 组织项目、部门和项目指标。
-- 返回投资收益率高于所有非空项目平均收益率的项目。
-- 显示项目编号、项目名称、部门名称、投资收益率、
-- 管理费和 roi_level。
-- 管理费为空时显示 0。
-- roi_level 对所有返回行显示“高于平均”。
-- 按投资收益率降序，项目编号升序。

-- 结果验证特征：
-- 1. 结果共 6 列。
-- 2. 投资收益率不得为 NULL。
-- 3. 每行收益率都严格高于非空收益率平均值。
-- 4. 每个项目最多一行。

-- 在下方填写 SQL：



-- ==================================================
-- 第 6 题：安全完成一次增、改、删练习（15 分）
-- ==================================================

-- 业务目标：
-- 只使用 department_id = 9301 的专用练习行。
-- 按安全顺序完成：
-- 1. 确认当前数据库；
-- 2. 在事务外预览并确认 9301 当前不存在；
-- 3. START TRANSACTION；
-- 4. 插入部门 9301，名称“独立测评部门”，城市“测试城市A”；
-- 5. SELECT 验证插入结果；
-- 6. UPDATE 前先 SELECT 预览同一行；
-- 7. 把城市修改为“测试城市B”；
-- 8. SELECT 验证修改结果；
-- 9. DELETE 前先 SELECT 预览同一行；
-- 10. 删除该行并 SELECT 验证已经不存在；
-- 11. ROLLBACK；
-- 12. 再次 SELECT，确认固定数据没有残留修改。

-- 安全门槛：
-- 1. 只能操作 mysql_beginner_practice。
-- 2. UPDATE 和 DELETE 必须有精确 WHERE。
-- 3. 必须以 ROLLBACK 结束，禁止 COMMIT。
-- 4. 禁止修改固定编号 1～20 的员工或编号 1～14 的项目。

-- 在下方填写完整 SQL：



-- 提交前自检：
-- [ ] 我没有打开 hints 或 answers
-- [ ] 我能逐句解释自己的 SQL
-- [ ] 我检查了列数和行数
-- [ ] 我检查了 NULL 和重复
-- [ ] 我检查了排序和无匹配数据
-- [ ] 第 6 题最终执行了 ROLLBACK
-- [ ] 最终 verify_database.sql 仍显示 PASS

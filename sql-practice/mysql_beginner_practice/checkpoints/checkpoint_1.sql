USE mysql_beginner_practice;

SELECT DATABASE() AS current_database;

-- 完成第 1～4 章后再做。每题独立思考，不要提前打开答案。

-- ==================================================
-- 题 1：筛选并稳定排序
-- ==================================================
-- 查询 2022-01-01 当天或之后入职的员工姓名、入职日期和薪资。
-- 按入职日期降序，相同日期按 employee_id 升序。
-- 应返回：
-- 3 列：employee_name、hire_date、salary；每名符合日期条件的员工一行。
-- 验证方式：
-- 检查所有日期都不早于边界，并检查主排序与并列排序是否稳定。

-- 在下方填写 SQL：



-- ==================================================
-- 题 2：AND 与 OR
-- ==================================================
-- 查询技术部（department_id = 2）中薪资至少 13000 的员工，
-- 或职位为“实习生”的员工。
-- 返回 employee_id、employee_name、department_id、job_title、salary。
-- 使用括号清楚表达条件。
-- 应返回：
-- 5 列；每名满足组合条件或职位条件的员工一行。
-- 验证方式：
-- 逐行判断是否满足完整业务条件，并检查没有因括号范围错误而多选或漏选。

-- 在下方填写 SQL：



-- ==================================================
-- 题 3：NULL 检查
-- ==================================================
-- 查询 end_date 为空且状态不是“已完成”的项目名称、状态和结束日期。
-- 应返回：
-- 3 列：project_name、project_status、end_date。
-- 验证方式：
-- 检查 end_date 全部为 NULL，并确认没有“已完成”项目。

-- 在下方填写 SQL：



-- ==================================================
-- 题 4：分组统计
-- ==================================================
-- 统计每个项目状态的项目数和平均预算。
-- 返回 project_status、project_count、average_budget。
-- 应返回：
-- 3 列；每种项目状态一行。
-- 验证方式：
-- 检查状态没有重复，并核对各组 project_count 之和是否等于项目总数。

-- 在下方填写 SQL：



-- ==================================================
-- 题 5：分组后筛选
-- ==================================================
-- 找出员工人数至少为 4 的部门编号。
-- 返回 department_id 和 employee_count。
-- 应返回：
-- 2 列；每个符合门槛的部门一行。
-- 验证方式：
-- 检查每组 employee_count 都不少于 4，且 department_id 没有重复。

-- 在下方填写 SQL：



-- ==================================================
-- 题 6：改错
-- ==================================================
-- 目标：统计 projects 的总行数。
-- 错误写法：
-- SELECT COUNT(end_date) AS project_count FROM projects;
-- 说明为什么结果不是项目总数，并改正。
-- 应返回：
-- 1 列 project_count、1 行，并附上文字解释。
-- 验证方式：
-- 把结果与 projects 的全部行数比较；解释必须说明 NULL 是否会影响原计数。

-- 在下方填写 SQL 和解释：

USE mysql_beginner_practice;

SELECT DATABASE() AS current_database;

-- 完成第 5～8 章后再做。每题独立思考，不要提前打开答案。

-- ==================================================
-- 题 1：INNER JOIN
-- ==================================================
-- 显示项目名称及其负责部门名称。
-- 应返回：
-- 2 列：project_name、department_name；每个有负责部门的项目一行。
-- 验证方式：
-- 抽查项目的 department_id 与部门名称是否来自正确关联，并检查异常重复。

-- 在下方填写 SQL：



-- ==================================================
-- 题 2：LEFT JOIN 和无匹配
-- ==================================================
-- 找出没有 project_metrics 的项目。
-- 返回 project_id 和 project_name。
-- 应返回：
-- 2 列；每个缺少指标记录的项目一行。
-- 验证方式：
-- 用 project_id 回查 project_metrics，确认每个结果都确实没有匹配行。

-- 在下方填写 SQL：



-- ==================================================
-- 题 3：避免重复计数
-- ==================================================
-- 统计每个部门参与过项目的不同员工人数。
-- 返回 department_id 和 distinct_employee_count。
-- 应返回：
-- 2 列；每个存在项目参与员工的部门一行。
-- 验证方式：
-- 检查同一员工参加多个项目时，在所属部门内仍只计一次。

-- 在下方填写 SQL：



-- ==================================================
-- 题 4：CASE WHEN
-- ==================================================
-- 显示项目名称和状态分类：
-- “已完成”归为“结束”，其他状态归为“未结束”。
-- 应返回：
-- 2 列：project_name 和状态分类；每个项目一行。
-- 验证方式：
-- 检查所有项目都出现一次、分类没有 NULL，并抽查两类项目。

-- 在下方填写 SQL：



-- ==================================================
-- 题 5：EXISTS
-- ==================================================
-- 查询至少有一名参与人员的项目编号和项目名称。
-- 应返回：
-- 2 列：project_id、project_name；每个符合条件的项目最多一行。
-- 验证方式：
-- 回查 employee_projects，确认每个结果至少有一条记录，且无人参与项目未出现。

-- 在下方填写 SQL：



-- ==================================================
-- 题 6：CTE、CAST 和 COALESCE
-- ==================================================
-- 使用非递归 CTE 先取得项目名称、ROI 和电压等级。
-- 最终把 ROI 转为 CHAR，并把 NULL 电压等级显示为“未填写”。
-- 应返回：
-- 3 列：项目名称、文本形式的 ROI、电压等级显示值；所有项目都应保留。
-- 验证方式：
-- 检查 ROI 的结果类型、NULL 电压等级的显示文本，以及项目是否遗漏或重复。

-- 在下方填写 SQL：

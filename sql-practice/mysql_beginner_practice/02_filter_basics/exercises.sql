USE mysql_beginner_practice;

SELECT DATABASE() AS current_database;

-- ==================================================
-- 练习 1：筛选技术部员工
-- ==================================================

-- 难度：模仿
-- 目标：
-- 查询 department_id 为 2 的员工姓名和部门编号。
-- 应返回：
-- 2 列：employee_name、department_id；只包含部门 2。
-- 使用知识：
-- WHERE、=
-- 验证方式：
-- 检查每行 department_id 是否都是 2，并检查列数。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：
select employee_name , department_id
from employees where department_id = 2;



-- ==================================================
-- 练习 2：筛选未结束项目
-- ==================================================

-- 难度：模仿
-- 目标：
-- 查询结束日期为空的项目名称和结束日期。
-- 应返回：
-- 2 列：project_name、end_date；返回行的 end_date 都为 NULL。
-- 使用知识：
-- IS NULL
-- 验证方式：
-- 检查结果中的 end_date，不要只看 SQL 是否运行。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：
select project_name , end_date
from projects
where end_date IS NULL;



-- ==================================================
-- 练习 3：筛选两个部门的高薪员工
-- ==================================================

-- 难度：基础变形
-- 目标：
-- 查询部门 1 或 3 中薪资至少 10000 的员工。
-- 显示 employee_name、department_id、salary。
-- 应返回：
-- 3 列；部门只可能是 1 或 3，薪资不低于 10000。
-- 使用知识：
-- IN、AND、数值条件
-- 验证方式：
-- 分别检查部门集合和薪资下限。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：
select employees.employee_name ,department_id ,employees.salary
from employees where department_id in (1,3) and salary >= 10000;



-- ==================================================
-- 练习 4：筛选指定预算范围的进行中项目
-- ==================================================

-- 难度：基础变形
-- 目标：
-- 查询预算在 600000 到 900000 之间且状态为“进行中”的项目。
-- 显示 project_name、project_status、budget。
-- 应返回：
-- 3 列；预算包含两个边界，状态全部为“进行中”。
-- 使用知识：
-- BETWEEN、AND、文本条件
-- 验证方式：
-- 检查预算边界和项目状态。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：

select project_name , project_status , budget
from projects where budget between 600000 and 900000
and project_status LIKE '%进行中%';


-- ==================================================
-- 练习 5：查找专员但排除市场部
-- ==================================================

-- 难度：基础变形
-- 目标：
-- 查询职位名称中包含“专员”，但 department_id 不是 4 的员工。
-- 显示 employee_name、department_id、job_title。
-- 应返回：
-- 3 列；职位包含“专员”，且没有部门 4。
-- 使用知识：
-- LIKE、NOT
-- 验证方式：
-- 检查文本包含关系和排除条件。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：
select employees.employee_name , department_id , employees.job_title
from employees where job_title LIKE '%专员%' AND department_id NOT IN (4);


-- ==================================================
-- 练习 6：制作重点项目筛选清单
-- ==================================================

-- 难度：独立
-- 目标：
-- 查询状态为“进行中”或“待启动”，且预算超过 800000 的项目。
-- 显示项目编号、名称、状态和预算。
-- 应返回：
-- 4 列；每行满足目标状态之一，并且预算严格大于 800000。
-- 使用知识：
-- AND、OR、括号
-- 验证方式：
-- 分开检查状态集合和预算条件，确认没有低预算行。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 7：查找已有结束日期的非完成项目
-- ==================================================

-- 难度：独立
-- 目标：
-- 查询 end_date 非空且状态不在“已完成”“暂停”中的项目。
-- 显示 project_name、project_status、end_date。
-- 应返回：
-- 3 列；无 NULL 结束日期，也没有被排除的状态。
-- 使用知识：
-- IS NOT NULL、NOT IN、AND
-- 验证方式：
-- 检查 NULL 和状态集合是否都正确。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 8：改正状态与预算筛选
-- ==================================================

-- 难度：改错
-- 目标：
-- 原意是查询“状态为进行中或待启动，并且预算至少 700000”的项目。
-- 错误 SQL（可以运行，但逻辑不正确）：
-- SELECT project_name, project_status, budget
-- FROM projects
-- WHERE project_status = '进行中'
--    OR project_status = '待启动'
--   AND budget >= 700000;
-- 请回答：
-- 1. 原 SQL 是否报错？
-- 2. 即使能运行，结果为什么可能不正确？
-- 3. 应如何修改？为什么？
-- 4. 修改后结果应有什么特征？
-- 应返回：
-- 3 列；两种状态都必须满足同一预算下限。
-- 使用知识：
-- AND 与 OR 优先级、括号
-- 验证方式：
-- 特别检查“进行中”项目是否混入低于 700000 的行。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写修改后的 SQL 和解释：



-- 本章结束自检：
-- [ ] 我先完成了 lesson.md 中的所有示例
-- [ ] 我没有看答案完成至少 6 道题
-- [ ] 我能解释每条 SQL 的作用
-- [ ] 我检查了返回列数
-- [ ] 我检查了返回行数
-- [ ] 我检查了 NULL 和重复
-- [ ] 我把真正做错的题记录进 mistake_log.md

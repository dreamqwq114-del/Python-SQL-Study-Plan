USE mysql_beginner_practice;

SELECT DATABASE() AS current_database;

-- ==================================================
-- 练习 1：插入专用练习部门
-- ==================================================

-- 难度：模仿
-- 目标：
-- 在事务中插入编号 9101、名称“练习一部门”、城市“南京”的部门。
-- 查询验证后默认 ROLLBACK，再查询确认专用行消失。
-- 应返回：
-- 回滚前 1 行，回滚后 0 行。
-- 使用知识：
-- INSERT、START TRANSACTION、ROLLBACK
-- 验证方式：
-- 两次按 department_id = 9101 查询。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 2：修改专用练习部门
-- ==================================================

-- 难度：模仿
-- 目标：
-- 先 SELECT 确认编号 9102 未被占用，再开始事务并插入专用部门。
-- UPDATE 前 SELECT 预览，把城市从“修改前”改为“修改后”，
-- 再 SELECT 验证，最后默认 ROLLBACK。
-- 应返回：
-- 更新前后只有目标行，最终不留下专用数据。
-- 使用知识：
-- UPDATE、安全预览、事务
-- 验证方式：
-- 检查主键条件、修改值和回滚。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 3：删除专用练习部门
-- ==================================================

-- 难度：基础变形
-- 目标：
-- 先 SELECT 确认编号 9103 未被占用，再开始事务并插入专用部门。
-- DELETE 前 SELECT 预览，删除后 SELECT 应为 0 行，最后 ROLLBACK。
-- 应返回：
-- 删除前 1 行，删除后 0 行；固定数据不变。
-- 使用知识：
-- DELETE、安全预览、事务
-- 验证方式：
-- 检查删除条件只命中 9103。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 4：插入专用练习员工
-- ==================================================

-- 难度：基础变形
-- 目标：
-- 在事务中插入员工 9104：姓名“练习员工”、department_id 为 5、
-- 职位“练习职位”、薪资 6000、入职日期 2026-01-15、manager_id 为 5。
-- 验证后默认 ROLLBACK。
-- 应返回：
-- 回滚前 1 行，回滚后 0 行。
-- 使用知识：
-- INSERT、日期值、外键、事务
-- 验证方式：
-- 检查所有列、外键值和回滚结果。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 5：修改专用练习项目状态
-- ==================================================

-- 难度：基础变形
-- 目标：
-- 先 SELECT 确认项目 9105 未被占用，再在事务中插入该项目，
-- 使用已有部门 1 和经理 6，
-- 自行填写其余必填字段，初始状态为“待启动”。
-- UPDATE 前预览，将状态改为“进行中”，验证后默认 ROLLBACK。
-- 应返回：
-- 更新只影响 9105，事务结束后不保留项目。
-- 使用知识：
-- INSERT、UPDATE、外键、安全事务
-- 验证方式：
-- 检查项目编号、状态、受影响行数和回滚。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 6：临时补充项目指标
-- ==================================================

-- 难度：独立
-- 目标：
-- 项目 14 当前没有指标记录。先 SELECT 确认，再在事务中插入：
-- metric_id 9114、project_id 14、管理费 0.12、收益率 0.0500、电压等级 10kV。
-- 验证后默认 ROLLBACK。
-- 应返回：
-- 插入后 1 行，回滚后仍无项目 14 的指标。
-- 使用知识：
-- INSERT、唯一键、外键、事务
-- 验证方式：
-- 按 project_id = 14 在事务前、事务中、回滚后检查。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 7：删除专用参与记录
-- ==================================================

-- 难度：独立
-- 目标：
-- 先 SELECT 确认组合键 9107、1 未被占用。
-- 再在一个事务中插入员工 9107，并插入其参与项目 1 的记录。
-- DELETE 前预览这条参与记录，只删除该组合主键，验证后默认 ROLLBACK。
-- 应返回：
-- 删除前 1 行，删除后 0 行；固定项目参与数据不变。
-- 使用知识：
-- INSERT、复合主键、DELETE、事务
-- 验证方式：
-- WHERE 同时限定 employee_id 和 project_id。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 8：识别危险的全表更新
-- ==================================================

-- 难度：改错
-- 目标：
-- 原意只是把员工 7 的薪资增加 500。判断下面 SQL：
-- 1. 是否报错；
-- 2. 即使能运行，结果是否正确；
-- 3. 应如何修改；
-- 4. 为什么这样修改；
-- 5. 修改后结果应有什么特征。
--
-- 危险 SQL（严禁实际执行）：
-- UPDATE employees
-- SET salary = salary + 500;
--
-- 应返回：
-- 只预览、修改和验证员工 7，最后默认 ROLLBACK。
-- 使用知识：
-- UPDATE、WHERE、安全事务
-- 验证方式：
-- 检查受影响行数为 1，并确认回滚后原薪资恢复。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方写判断、安全修改 SQL 和原因：


-- 本章结束自检：
-- [ ] 我先完成了 lesson.md 中的所有示例
-- [ ] 我没有看答案完成至少 6 道题
-- [ ] 我能解释每条 SQL 的作用
-- [ ] 我检查了返回列数
-- [ ] 我检查了返回行数
-- [ ] 我检查了 NULL 和重复
-- [ ] 我把真正做错的题记录进 mistake_log.md

USE mysql_beginner_practice;

SELECT DATABASE() AS current_database;

-- ==================================================
-- 练习 1：统计每个部门的项目数
-- ==================================================

-- 难度：模仿
-- 目标：
-- 显示部门编号、部门名称和项目数，保留没有项目的部门。
-- 按部门编号升序。
-- 应返回：
-- 每个部门一行，3 列；无项目部门计数为 0。
-- 使用知识：
-- LEFT JOIN、COUNT、GROUP BY
-- 验证方式：
-- 项目数总和应与项目总数一致，并检查无匹配部门。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 2：制作项目经理与预算清单
-- ==================================================

-- 难度：模仿
-- 目标：
-- 显示项目编号、名称、经理姓名和预算。
-- 无经理时显示“未分配”，按预算降序、项目编号升序。
-- 应返回：
-- 每个项目一行，4 列；所有项目都保留。
-- 使用知识：
-- LEFT JOIN、COALESCE、ORDER BY
-- 验证方式：
-- 检查无经理项目、排序和重复。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 3：按项目状态汇总预算
-- ==================================================

-- 难度：基础变形
-- 目标：
-- 按项目状态统计项目数和预算总额。
-- 只保留项目数不少于 3 的状态，按预算总额降序、状态升序。
-- 应返回：
-- 每种符合条件的状态一行，共 3 列。
-- 使用知识：
-- GROUP BY、COUNT、SUM、HAVING、ORDER BY
-- 验证方式：
-- 抽查状态明细，确认 HAVING 在分组后筛选。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 4：找出参与人数较多的项目
-- ==================================================

-- 难度：基础变形
-- 目标：
-- 显示项目编号、名称、状态和不重复参与人数。
-- 只保留参与人数不少于 3 的项目，
-- 按参与人数降序、项目编号升序。
-- 应返回：
-- 每个符合条件的项目一行，共 4 列。
-- 使用知识：
-- LEFT JOIN、COUNT(DISTINCT)、GROUP BY、HAVING
-- 验证方式：
-- 对照 employee_projects 明细，检查重复计数。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 5：标记项目进度风险
-- ==================================================

-- 难度：基础变形
-- 目标：
-- 显示项目编号、名称、状态、结束日期和风险标签。
-- 暂停项目标记“需关注”；结束日期为空标记“日期待定”；
-- 其他项目标记“正常检查”。按项目编号升序。
-- 应返回：
-- 所有项目，每个项目一行，共 5 列。
-- 使用知识：
-- CASE WHEN、NULL、排序
-- 验证方式：
-- 分别抽查暂停、结束日期为空和其他项目。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 6：制作部门经营概览
-- ==================================================

-- 难度：独立
-- 目标：
-- 每个部门显示部门编号、名称、项目数和项目预算总额。
-- 保留没有项目的部门，缺失总预算显示 0。
-- 按预算总额降序、部门编号升序。
-- 应返回：
-- 每个部门一行，共 4 列。
-- 使用知识：
-- LEFT JOIN、COUNT、SUM、COALESCE、GROUP BY
-- 验证方式：
-- 项目数总和与项目总数一致，预算总额不因员工连接而重复。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 7：查找资料不完整的项目
-- ==================================================

-- 难度：独立
-- 目标：
-- 找出“没有参与人员”或“没有项目指标”的项目。
-- 显示项目编号、名称、状态，以及两个说明列：
-- 人员缺失时显示“无人参与”，否则“已有人员”；
-- 指标缺失时显示“无指标”，否则“已有指标”。
-- 每个项目只出现一行，按项目编号升序。
-- 应返回：
-- 只包含至少缺少一种资料的项目，共 5 列。
-- 使用知识：
-- LEFT JOIN 或 EXISTS、CASE、NULL、重复检查
-- 验证方式：
-- 分别反查参与表和指标表，确认条件是 OR。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：


-- ==================================================
-- 练习 8：修复被重复放大的预算
-- ==================================================

-- 难度：改错
-- 目标：
-- 原意是统计每个项目的预算，但下面查询连接了参与表后直接求和。
-- 判断：
-- 1. 是否报错；
-- 2. 即使能运行，结果是否正确；
-- 3. 应如何修改；
-- 4. 为什么这样修改；
-- 5. 修改后结果应有什么特征。
--
-- 错误 SQL（只阅读，不要把它当作正确答案）：
-- SELECT
--     p.project_id,
--     p.project_name,
--     SUM(p.budget) AS project_budget
-- FROM projects AS p
-- JOIN employee_projects AS ep
--     ON ep.project_id = p.project_id
-- GROUP BY p.project_id, p.project_name;
--
-- 应返回：
-- 所有项目，每个项目一行，预算等于 projects 中的原预算。
-- 使用知识：
-- 一对多 JOIN、重复、正确分析粒度
-- 验证方式：
-- 抽查项目 1，并确认无参与人员项目没有丢失。
-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方写判断、修改后的 SQL 和原因：


-- 本章结束自检：
-- [ ] 我先完成了 lesson.md 中的所有示例
-- [ ] 我没有看答案完成至少 6 道题
-- [ ] 我能解释每条 SQL 的作用
-- [ ] 我检查了返回列数
-- [ ] 我检查了返回行数
-- [ ] 我检查了 NULL 和重复
-- [ ] 我把真正做错的题记录进 mistake_log.md

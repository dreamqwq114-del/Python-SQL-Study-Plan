# 最终审计摘要

> 状态说明：本文主体记录的是 2026-07-30 及更早的历史审计。2026-08-13 首次复核时 MariaDB 未启动并返回 `ERROR 2002 / 10061`；随后已启动本地服务并重新完成当前实跑验证。

2026-08-13 阶段 4 已补充 Python 无提示独立测评，以及 `12_python_sql_bridge/` 的只读 SQL–Python 桥接任务；它们是测评材料，不代表学习者已经通过测评。阶段 5 在 MariaDB 10.4.32 上实跑了第 1～10 章参考答案、两个 checkpoint 和独立测评答案共 13 份脚本，全部退出码为 0，且每份执行后固定基线均为 `PASS`。桥接查询验证为 14 行、14 个不同项目，其中 2 个项目没有参与人员。

## 结论

- 重构完成。
- 第 1 章先作为样板完成，并经过独立教学 Subagent 审计、修复和复核后，才展开其他章节。
- Subagent 功能成功启用。
- 截至第三轮，共调用 8 个独立 Subagent：原重构阶段 6 个，第三轮新增“术语与初学者理解”和“运行环境、SQL 正确性与测评有效性”2 个。
- 第三轮第一轮双审计合计：严重 0、一般 3、建议 3。
- 第三轮最终未解决：严重 0、一般 0、建议 1。
- 历史环境结论：2026-07-30 的 XAMPP MariaDB 与 IDEA 数据源已经就绪，可以运行本学习库。
- 教材结论：术语、学习依赖、示例、练习、答案和无提示测评已形成独立实践闭环。
- 个人能力结论：尚未提交第 11 章实际作答，因此个人独立能力尚未验证。

## 备份

- 备份目录：`backup_before_refactor/`。
- 备份文件数：34。
- 建立备份时，源文件与备份文件数量一致，SHA-256 不一致文件数为 0。
- 旧版根目录重复文件已从新结构中移除；完整旧内容仍保存在备份目录，可恢复。

## 第 1 章样板审计

- 独立角色：SQL 基础不牢大学生视角。
- 第一次发现：严重 0、一般 5、建议 2。
- 修复内容：拆分概念、重新设计练习梯度、修复提示泄题、增强答案验证、调整示例数量和时间说明。
- 后续最终教学审计又要求补充 `SELECT *` 可运行观察示例，现已完成。
- 样板最终状态：内容审计合格。

## 最终第一轮双审计

### MySQL 正确性审计

- 严重：0。
- 一般：1。
- 建议：2。

主要发现：

- setup 的初步状态曾用 `>=` 检查行数，与固定基线的精确检查不一致；
- `information_schema.tables.table_rows` 对 InnoDB 是估算值；
- `VALUES(column)` 在较新的 MySQL 8.0 中已标记弃用，但目前仍是有效语法。

### 初学者教学质量审计

- 严重：0。
- 一般：5。
- 建议：0。

主要发现：

- 45～60 分钟与 8 题每题至少 5～10 分钟存在时间下限冲突；
- 诊断页曾引用不存在的普通章节文件；
- 第 2 章曾在一个示例中同时引入 `IN`、`BETWEEN`、`LIKE`；
- `LIKE` 的一处预测方向写反；
- 第 1 章 `SELECT *` 和第 6 章 LEFT JOIN 右表筛选陷阱缺少独立操作脚手架。

### 第一轮合计

- 严重：0。
- 一般：6。
- 建议：2。

## 主 Agent 修复

- 修复全部 6 个一般问题。
- 采纳并修复 1 条建议：不再展示 InnoDB 的估算行数，只列出表名并用 `COUNT(*)` 精确核对。
- 共修复 7 个第一轮问题或建议。
- 未采纳 1 条建议：继续使用 `VALUES(column)` 完成幂等基线写入。

未采纳原因：

- 该写法在 MySQL 8.0 中仍可执行，只是被标记为弃用；
- 当前可用实测环境 MariaDB 10.4.32 不支持建议替换的新式行别名写法；
- 保留当前写法可以让同一 setup 脚本在当前 XAMPP 环境和 MySQL 8.0 目标语法之间保持可运行；
- 未来若课程只运行在较新 MySQL 8.0，可再迁移到新式别名语法。

## 第二轮正确性复核

- 第一轮一般问题已修复。
- 已采纳的 `table_rows` 建议已修复。
- 54 个 lesson 示例与 54 个最小修改答案一一对应。
- 80 道正式练习与 80 份练习答案一一对应。
- 第二轮新增问题：0。
- 最终未解决：严重 0、一般 0、建议 1。

## 实际数据库运行

- 实际连接：成功。
- 客户端：`D:\xampp\mysql\bin\mysql.exe`。
- 实际服务器兼容环境：XAMPP MariaDB 10.4.32。
- 实际数据库：仅 `mysql_beginner_practice`。
- 第二轮独立复跑：71 个执行单元。
- SQL 报错：0。
- 最终 `baseline_status`：`PASS`。
- 第 9 章运行后固定数据无残留修改。
- 未使用 `DROP DATABASE`，未操作其他数据库。

重要边界：

- 本机没有发现真正的 MySQL 8.0 服务。
- 因此不能声称“已在真实 MySQL 8.0 中全部运行通过”。
- 当前结论是：MySQL 8.0 静态方言审计通过；MariaDB 10.4.32 实际执行通过。

## 教学复核

- 第一轮 5 个一般教学问题全部确认修复。
- 第 1、2、6 章分别有 6、7、6 个示例，其余章节各有 5 个，均在 5～8 个范围内。
- 每个示例均保留预测、观察、最小修改和对应答案。
- 每章 8 道题、2/3/2/1 分层、8 组三级提示和 8 份答案未受影响。
- 新增教学问题：0。
- 最终未解决：严重 0、一般 0、建议 0。

## 当前限制

1. 没有真实 MySQL 8.0 运行环境，只完成 MySQL 8.0 静态审计和 MariaDB 10.4.32 实跑。
2. 整章通常需要 65～90 分钟。一次学习时段控制在 45～60 分钟，需要分两个时段；这是“8 题且每题至少思考 5～10 分钟”和“整章 45～60 分钟”之间的时间下限冲突。
3. setup 中的 `VALUES(column)` 是有效但已弃用的 MySQL 8.0 写法，保留用于当前 MariaDB/MySQL 双环境兼容。

## 第一天只打开

1. `00_setup/lesson.md`
2. `00_setup/create_database.sql`
3. `00_setup/verify_database.sql`
4. `00_diagnostic/lesson.md`
5. `00_diagnostic/diagnostic.sql`

第一天不要打开全部章节，也不要提前打开任何 `answers.sql`。

# 第三轮：术语、环境与独立实践验收

## 第三轮修改前备份

- 备份目录：`backup_before_readiness_audit/`；
- 备份对象：第三轮修改前的 57 个当前教材文件；
- 备份文件数：57；
- 建立备份时源文件与备份文件数量一致，SHA-256 不一致文件数为 0；
- 原 `backup_before_refactor/` 继续保留，本轮未删除旧资料。

## 本轮新增与修订

- 新增根目录 `glossary.md`，覆盖基本对象、筛选、聚合、JOIN、子查询、CTE、类型、事务和经营分析名词；
- README 增加提前可见的术语入口和完整依赖路线；
- README 增加本机 IDEA 新数据源的实际配置和 MariaDB/MySQL 8.0 边界；
- `progress_tracker.md` 增加第 11 章测评记录；
- 新增 `11_independent_readiness/`，包含说明、空白作答文件、评分规则和独立答案文件，不含 `hints.md`；
- 新增两份独立第三轮审计报告：
  - `audit_terms_and_learning_readiness.md`；
  - `audit_environment_and_assessment.md`。

## IDEA 和依赖实证

- XAMPP 进程：`D:\xampp\mysql\bin\mysqld.exe`；
- 监听端口：`3306`；
- 实际服务器：MariaDB `10.4.32`；
- IDEA 驱动：MariaDB Connector/J `3.5.7`，JAR 已存在于 IDEA 的 `jdbc-drivers` 目录；
- 旧数据源 `sql_beginner_practice@localhost` 保留；
- 新数据源 `mysql_beginner_practice@localhost` 已增加；
- JDBC URL：`jdbc:mariadb://localhost:3306/mysql_beginner_practice`；
- User：`root`；
- 两份 IDEA 数据源 XML 均通过 XML 解析检查。

主 Agent 已在 IDEA 界面实际完成：

1. Test Connection，结果为成功；
2. 核对 DBMS 为 MariaDB `10.4.32`；
3. 核对驱动为 MariaDB Connector/J `3.5.7`；
4. 在新数据源的 Query Console 执行版本、当前数据库和五表计数查询；
5. 结果显示当前库为 `mysql_beginner_practice`，五表行数为 `5 / 20 / 14 / 12 / 37`。

因此当前 IDEA 可以直接使用该练习库。该证据证明的是 MariaDB 连接，不是真实 MySQL 8.0 服务端。

## 第三轮实际运行

- 普通模式逐单元执行：72 个单元，72 PASS，0 FAIL；
- 其中第 1～10 章 lesson 示例：54 / 54 PASS；
- setup、verify、诊断答案、十章答案、两个 checkpoint 答案和第 11 章答案均通过；
- 强制 `ONLY_FULL_GROUP_BY` 等严格模式：68 / 68 PASS；
- 第 9 章答案后固定基线：PASS；
- 第 11 章答案后固定基线：PASS；
- 第 11 章第 6 题运行后，`department_id = 9301` 不存在；
- 未执行 `DROP DATABASE`，未操作其他数据库；
- 可执行的 `UPDATE` 和 `DELETE` 均有 `WHERE`。

方言边界：

- SQL Server、PostgreSQL 专有语法和旧数据库名扫描均无命中；
- MySQL 8.0 静态方言及严格分组检查通过；
- 本机没有真实 MySQL 8.0 服务，不能声称“真实 MySQL 8.0 全量实跑通过”。

## 第三轮第一轮双审计

### 术语与初学者理解审计

- 严重：0；
- 一般：2；
- 建议：2。

发现并修复：

- 术语表入口出现过晚；
- 独立测评未明确禁止计时期间查术语表；
- “连接键”缺少同名解释；
- 数据类型缺少 `DESCRIBE` 只读观察方法和学习范围边界。

### 环境、SQL 与测评有效性审计

- 严重：0；
- 一般：1；
- 建议：1。

一般问题是第 11 章原第 4 题的固定数据无法区分正确 `OR` 和错误 `AND`。现已改为“无人参与或投资收益率为空”：

- 正确逻辑返回项目 `12、13、14`；
- 错误 `AND` 只返回 `13、14`；
- 项目 12 是“有参与人员但收益率缺失”的可观察反例；
- 题目、评分规则和答案已同步；
- 普通模式、严格模式和最终基线均再次通过。

### 第一轮合计

- 严重：0；
- 一般：3；
- 建议：3；
- 第一轮 6 项中修复 5 项，保留 1 项为已知兼容限制。

## 第二轮和最终复核

- 两个第三轮 Subagent 均完成第二轮复核；
- 第一轮 3 个一般问题全部确认修复；
- 第一轮 2 个教学建议已采纳并确认修复；
- 第二轮新增 1 个轻微提示泄漏建议，已删除题面中的 SQL 关键字并再次确认；
- 术语教学审计最终未解决：严重 0、一般 0、建议 0；
- 运行与正确性审计最终未解决：严重 0、一般 0、建议 1；
- 第三轮共修复 6 个问题或建议。

唯一未采纳建议：

- setup 的 `VALUES(column)` 在 MySQL 8.0.20 之后被标记为弃用；
- 它在 MySQL 8.0 中仍可执行，并在当前 MariaDB 10.4.32 实跑通过；
- 改用较新 MySQL 的行别名写法会降低当前 XAMPP MariaDB 的兼容性；
- 因此本轮保留并记录限制，未来只使用真实新版 MySQL 8.0 时再迁移并复跑。

## 独立实践测评

- 题目：6；
- 总分：100；
- 限时：90 分钟；
- 通过线：80；
- 第 6 题设安全门槛；
- 题目只给业务目标、返回列和验证特征；
- 没有 `hints.md`，空白作答区没有 SQL 骨架；
- 参考答案单独保存并已实跑通过。

能力结论必须分开：

- 环境已就绪：是；
- 教材足以形成基础 SQL 独立实践闭环：是；
- 学习者本人已经证明能独立完成：否，尚未提交实际测评作答。

只有提交自己的作答文件、得分至少 80，并且第 6 题没有错误数据库、无 `WHERE` 修改、遗漏回滚或其他安全违规，才能判定个人通过。

## 第三轮后的已知限制

1. 实跑环境是 MariaDB 10.4.32，不是真实 MySQL 8.0；
2. 完整一章合理需要 65～90 分钟，建议拆成两个 45～60 分钟学习时段；
3. setup 保留了兼容 MariaDB 的 `VALUES(column)` 写法，较新 MySQL 8.0 会给弃用提示；
4. DBeaver 本轮未做界面验证；IDEA 数据源已经实际验证。

## 仍然建议第一天只打开

1. `00_setup/lesson.md`
2. `00_setup/create_database.sql`
3. `00_setup/verify_database.sql`
4. `00_diagnostic/lesson.md`
5. `00_diagnostic/diagnostic.sql`

不要第一天打开全部章节。完成第 1～10 章和两个 checkpoint 后，才进入 `11_independent_readiness/assessment.sql`。

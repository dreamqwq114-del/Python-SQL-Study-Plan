USE mysql_beginner_practice;

SELECT DATABASE() AS current_database;

-- ==================================================
-- 练习 1：按薪资给员工分类
-- ==================================================

-- 难度：模仿

-- 目标：
-- 显示员工姓名、薪资和 salary_level。
-- 薪资不低于 15000 标记为“较高”，否则标记为“普通”。
-- 按员工编号升序排列。

-- 应返回：
-- 3 列：employee_name、salary、salary_level。
-- 每名员工一行。

-- 使用知识：
-- CASE WHEN、AS

-- 验证方式：
-- 抽查等于或接近 15000 的值。
-- 检查每行是否都有分类。

-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 2：标记电压等级是否缺失
-- ==================================================

-- 难度：模仿

-- 目标：
-- 显示项目指标编号、电压等级和 voltage_status。
-- 电压等级为 NULL 时标记“缺失”，否则标记“已填写”。
-- 按指标编号升序排列。

-- 应返回：
-- 3 列：metric_id、voltage_level、voltage_status。
-- 每条指标记录一行。

-- 使用知识：
-- CASE WHEN、IS NULL

-- 验证方式：
-- 分别抽查 NULL 和非 NULL 行。
-- 确认原电压字段没有被修改。

-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 3：按管理费分类
-- ==================================================

-- 难度：基础变形

-- 目标：
-- 显示项目编号、管理费和 fee_level。
-- 管理费为 NULL 标记“未知”；不低于 0.20 标记“较高”；其他标记“普通”。
-- 按项目编号升序排列。

-- 应返回：
-- 3 列：project_id、management_fee、fee_level。
-- 每条项目指标记录一行。

-- 使用知识：
-- CASE WHEN、多分支、NULL

-- 验证方式：
-- 抽查 NULL、0.20 和低于 0.20 的记录。
-- 检查分支顺序是否覆盖全部情况。

-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 4：查询预算高于平均值的项目
-- ==================================================

-- 难度：基础变形

-- 目标：
-- 显示项目编号、项目名称和预算。
-- 只保留预算严格高于所有项目平均预算的项目。
-- 按预算降序、项目编号升序排列。

-- 应返回：
-- 3 列：project_id、project_name、budget。
-- 每行预算都严格高于平均预算。

-- 使用知识：
-- 标量子查询、AVG、WHERE

-- 验证方式：
-- 先单独运行平均预算子查询。
-- 检查返回的最低预算仍高于平均值。

-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 5：查询有高预算项目的部门
-- ==================================================

-- 难度：基础变形

-- 目标：
-- 显示部门编号和部门名称。
-- 只保留至少有一个预算不低于 1000000 项目的部门。
-- 按部门编号升序排列。

-- 应返回：
-- 2 列：department_id、department_name。
-- 每个符合条件的部门一行。

-- 使用知识：
-- IN 子查询、WHERE

-- 验证方式：
-- 单独运行子查询检查部门编号。
-- 确认外层部门没有因多个项目重复。

-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 6：制作项目进度标签
-- ==================================================

-- 难度：独立

-- 目标：
-- 显示项目编号、项目名称、项目状态和 progress_label。
-- 状态为“已完成”标记“已结束”；
-- 否则，结束日期为 NULL 标记“日期待定”；
-- 其余标记“有计划日期”。
-- 按项目编号升序排列。

-- 应返回：
-- 4 列：project_id、project_name、project_status、progress_label。
-- 每个项目一行。

-- 使用知识：
-- CASE WHEN、条件顺序、NULL

-- 验证方式：
-- 分别抽查三个分支。
-- 检查已完成且有日期的项目是否先命中第一分支。

-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 7：查询高收益项目所属部门的员工
-- ==================================================

-- 难度：独立

-- 目标：
-- 显示员工编号、员工姓名和部门编号。
-- 只保留所在部门拥有投资收益率不低于 0.0850 项目的员工。
-- 按员工编号升序排列。

-- 应返回：
-- 3 列：employee_id、employee_name、department_id。
-- 每名符合条件的员工一行。

-- 使用知识：
-- IN 子查询、INNER JOIN、WHERE

-- 验证方式：
-- 先验证子查询只返回一列部门编号。
-- 检查外层员工是否重复以及部门是否符合条件。

-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写 SQL：



-- ==================================================
-- 练习 8：改正返回多行的标量子查询
-- ==================================================

-- 难度：改错

-- 目标：
-- 原意是查询薪资高于公司平均薪资的员工。
-- 判断下面 SQL 是否报错、结果是否正确，并修复。

-- 错误 SQL（子查询返回多行）：
-- SELECT employee_name, salary
-- FROM employees
-- WHERE salary > (
--     SELECT salary
--     FROM employees
-- );

-- 请回答：
-- 1. 原 SQL 是否会报错？
-- 2. 子查询为什么不能作为一个值比较？
-- 3. 应如何修改？为什么？
-- 4. 修改后结果应有什么特征？

-- 应返回：
-- 2 列：employee_name、salary。
-- 每行薪资严格高于公司平均薪资。

-- 使用知识：
-- 标量子查询、AVG、子查询返回行数

-- 验证方式：
-- 单独运行修正后的子查询，确认一行一列。
-- 检查外层每个薪资都高于该值。

-- 提示：
-- 卡住后打开本章 hints.md；不要提前打开 answers.sql。
-- 在下方填写修改后的 SQL 和解释：



-- 本章结束自检：
-- [ ] 我先完成了 lesson.md 中的所有示例
-- [ ] 我没有看答案完成至少 6 道题
-- [ ] 我能解释每条 SQL 的作用
-- [ ] 我检查了返回列数
-- [ ] 我检查了返回行数
-- [ ] 我检查了 NULL 和重复
-- [ ] 我把真正做错的题记录进 mistake_log.md

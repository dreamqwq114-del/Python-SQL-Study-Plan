# 诊断复盘

先不要看 `answers.sql`。根据自己的 SQL 和结果完成自评。

## 每题评分

- 2 分：独立写出，并检查了结果特征；
- 1 分：思路基本正确，但语法、字段或结果验证有问题；
- 0 分：不会，或结果明显不符合题意。

总分 16 分。分数只用于安排学习速度。

## 结果解释

- 0～5 分：从第 1 章开始，逐个示例操作；
- 6～10 分：仍从第 1 章开始，重点记录容易忘的语法；
- 11～13 分：按顺序复习，独立题和改错题不能跳过；
- 14～16 分：基础较完整，但仍需完成检查点和综合练习。

## 按知识点复盘

- 第 1 题：能否只返回题目需要的列？
- 第 2 题：能否同时处理筛选、排序和并列顺序？
- 第 3 题：是否使用 `IS NULL`，而不是 `= NULL`？
- 第 4 题：是否正确使用 `COUNT` 和 `GROUP BY`？
- 第 5 题：JOIN 条件是否连接了正确的主键和外键？
- 第 6 题：是否保留了左表记录，并正确查找无匹配行？
- 第 7 题：`CASE` 是否为每个项目产生一个分类？
- 第 8 题：是否坚持在修改前先用相同条件 `SELECT`？

## 我的薄弱章节

- 最需要慢学的章节：
- 最容易混淆的概念：
- 我最担心的结果检查：

填写完成后，才打开 `answers.sql`。答案只用于核对，不要覆盖自己的原始作答。
